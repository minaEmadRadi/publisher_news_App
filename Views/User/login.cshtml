
@using MVCLab04.Models
@model user
@{
    ViewBag.Title = "Login";
    Layout = "~/Views/Shared/_Layout.cshtml";
}


<div class="container justify-content-around ">
    <h2 class="col-8 text-center">Login</h2>
    <form method="post">
        <table class="table table-dark justify-content-center">

            <tr>
                <td>@Html.LabelFor(n => n.email)</td>
                <td>@Html.TextBoxFor(n => n.email)</td>
                <td>@Html.ValidationMessageFor(n => n.email)</td>
            </tr>
            <tr>
                <td>@Html.LabelFor(n => n.password)</td>
                <td>@Html.PasswordFor(n => n.password)</td>
                <td>@Html.ValidationMessageFor(n => n.password)</td>
            </tr>
            <tr>
             <td></td> <td>@Html.CheckBox("remb") @Html.Label("Remember Me") </td>
             
                <td>@if (ViewBag.mess != null) {@Html.Label((string)@ViewBag.mess)}</td>

            </tr>

            <tr>
                <td>
                    <input type="submit" value="Save" class="badge-primary col-8 offset-11" />
                </td>
            </tr>
        </table>

    </form>
</div>

